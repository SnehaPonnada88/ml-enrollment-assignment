[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ml-enrollment-assignment"
version = "0.1.0"
requires-python = ">=3.10"

[tool.setuptools]
packages = ["src", "app"]
